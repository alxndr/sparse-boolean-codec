# PLAN: sparse-boolean-codec

Working list of what's still in progress. Durable project docs live
elsewhere: README.md (usage/API/gotchas), PUBLISHING.md (release
process), CHANGELOG.md (release history), and `docs/architecture-decisions/`
(why things are the way they are). Once nothing's left below, this file
goes away.

- [ ] Finish reviewing docs/comments for accuracy against the current
      implementation (in progress).
- [ ] Release `2.0.0-alpha`: finish committing, tag `v2.0.0-alpha`, push,
      confirm `publish.yml` succeeds via Trusted Publishing.
- [ ] Promote to a real `2.0.0` once satisfied, following `PUBLISHING.md`.
- [ ] Split `src/index.ts` into separate files along natural seams
      (raised, not yet scoped).
